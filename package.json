{
  "name": "pay-per-prompt",
  "version": "1.0.0",
  "description": "Pay-per-prompt AI on Hedera: every answer is settled with an HTS HBAR payment via the x402 exact scheme, logged to a public HCS audit ledger, with the ledger topic pinned by a deployed Solidity registry. Next.js + Hardhat monorepo.",
  "license": "MIT",
  "private": true,
  "packageManager": "npm@10.8.2",
  "engines": {
    "node": ">=20.18.3"
  },
  "workspaces": [
    "packages/contracts",
    "packages/frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w @pay-per-prompt/frontend",
    "build": "npm run build -w @pay-per-prompt/frontend",
    "start": "npm run start -w @pay-per-prompt/frontend",
    "lint": "npm run lint -w @pay-per-prompt/frontend && npm run lint -w @pay-per-prompt/contracts",
    "typecheck": "npm run typecheck -w @pay-per-prompt/frontend && npm run typecheck -w @pay-per-prompt/contracts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "compile": "npm run compile -w @pay-per-prompt/contracts",
    "test": "npm run test -w @pay-per-prompt/contracts",
    "test:testnet": "npm run test:testnet -w @pay-per-prompt/contracts",
    "setup:hedera": "npm run setup -w @pay-per-prompt/contracts",
    "deploy:registry": "npm run deploy -w @pay-per-prompt/contracts"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
